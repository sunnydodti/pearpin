name: pearpin
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: 3.6.0
  flutter: 3.27.1

dependencies:
  flutter:
    sdk: flutter
  hive_ce: ^2.10.1
  hive_ce_flutter: ^2.2.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
